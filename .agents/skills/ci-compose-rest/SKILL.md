---
name: ci-compose-rest
description: Manage CI Compose deployments through the local REST API, restricted to the selected configuration scope.
---

# CI Compose REST

This is the repository-discovery entry point for CI Compose. Before performing
any task with this skill, read and follow the complete canonical instructions
at `skills/ci-compose-rest/SKILL.md` from the repository root.

Keep the operation within the boundaries defined there. In particular, use the
local CI Compose REST API as the exclusive OCI access path; do not use OCI CLI
commands directly.
